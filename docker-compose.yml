services:
  heroscapebuilder:
    pull_policy: build
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8012:80"
      - "8043:5173"
    environment:
      - ASPNETCORE_URLS=http://+
      - HeroscapeBuilder=${HeroscapeBuilder}