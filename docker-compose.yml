services:
  heroscapebuilder:
    build:
      context: .
      dockerfile: Dockerfile
      pull_policy: build
    ports:
      - "8012:80"
    environment:
      - ASPNETCORE_URLS=http://+
      - HeroscapeBuilder=${HeroscapeBuilder}