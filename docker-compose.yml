# See .env file for passwords to SQL and Minio
services:
  heroscapebuilder:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - ASPNETCORE_URLS=http://+
      - HeroscapeBuilder=${HeroscapeBuilder}
#      - HeroscapeBuilder=/app/config/HeroscapeBuilder.config.json
#    volumes:
#      - ./HeroscapeBuilder.config.json:/app/config/HeroscapeBuilder.config.json
#    depends_on:
#      - mssql
    networks:
      - internal_network

#  mssql:
#    image: mcr.microsoft.com/mssql/server:2022-latest
#    environment:
#      - ACCEPT_EULA=Y
#      - SA_PASSWORD=${MSSQL_SA_PASSWORD}
#      - MSSQL_PID=Express
#    volumes:
#      - mssql_data:/var/opt/mssql
#    networks:
#      - internal_network

# I am using a single instances of minio instead of one inside docker      
#  minio:
#    image: minio/minio
#    command: server --console-address ":9081" /data  # Set WebUI to port 9001
#    ports:
#      - "9000:9080"  # Expose API
#      - "9081:9081"  # Expose WebUI
#    environment:
#      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
#      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
#      - MINIO_BROWSER=on
#    volumes:
#      - minio_data:/data
#    networks:
#      - internal_network

networks:
  internal_network:
    driver: bridge

#volumes:
#  mssql_data:
#    driver: local
#  minio_data:
#    driver: local  
