services:
  heroscapebuilder:
    pull_policy: build
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8012:80"
    environment:
      - ASPNETCORE_URLS=http://+
      - HeroscapeBuilder=${HeroscapeBuilder}